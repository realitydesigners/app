---
import BlogPost from './BlogPost.astro'

export interface Props {
  linkedContent: any[]
}

const { linkedContent } = Astro.props
---

{
  linkedContent?.map((item: any) => {
    return <BlogPost post={item.post} />
  })
}
